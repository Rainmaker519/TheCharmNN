#the weights are between each layer
#and the biases are for all layers
#except the input, so both are
#(n_layers-1) elements. {the [L]s}
[l_sizes]2,2,1[\l_sizes]
[w].014,-.2|-.05,.12[\w]
[b]0,0[\b]
[w].1|.3[\w]
[b]0[\b]
[end]
